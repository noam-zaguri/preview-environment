Preview environment "{{ .Release.Name }}" deployed to namespace {{ .Release.Namespace }}.

{{- if .Values.route.enabled }}

URL:
{{- if .Values.route.tls.enabled }}
  https://{{ .Values.route.host | default (printf "%s-%s.<cluster-apps-domain>" (include "preview-dashboard.fullname" .) .Release.Namespace) }}
{{- else }}
  http://{{ .Values.route.host | default (printf "%s-%s.<cluster-apps-domain>" (include "preview-dashboard.fullname" .) .Release.Namespace) }}
{{- end }}

Run this to get the actual host OpenShift assigned:
  oc get route {{ include "preview-dashboard.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.host}'
{{- end }}
